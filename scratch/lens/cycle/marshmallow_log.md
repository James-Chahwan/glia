# Marshmallow-1359 iteration log

Cycle 0 loop until gold passes. Newest at top. Append-only.

## iter5 — graph+issue auto-derived directive · 2026-05-21 ~07:5X · 🟢 PASS

**The real glia win.** Zero hand-written instance-specific text. Suffix directive auto-derived from .gmap POSITION cells + issue.txt regex parsing.

**Channels added (cumulative iter3→iter4→iter5):**
- iter3: graph-derived candidate list from traceback POSITION matches
- iter4: deepest-frame target selection + anti-target list (4 NOT-this functions)
- **iter5**: + buggy source-line text (parsed from traceback excerpt in issue) + exception class/receiver-type/missing-attribute (parsed from `AttributeError: 'List' object has no attribute 'opts'`) + substitution hint pointing at access-paths section

**Directive content (verbatim, auto-generated by synth_traceback_target.rs):**

```
## Required fix target (graph-derived from issue traceback)

Edit ONE function: `src::marshmallow::fields::DateTime::_bind_to_schema` in `src/marshmallow/fields.py` (function body spans lines 1112-1118).
The SWE-bench issue traceback names this function at line 1117 (the DEEPEST frame — where the exception throws).
The BUGGY LINE (per traceback line 1117) is:
    or getattr(schema.opts, self.SCHEMA_OPTS_VAR_NAME)
The exception is `AttributeError: 'List' object has no attribute 'opts'`. ...
Replace the failing `<X>.opts` access with the access-path that actually resolves to `.opts` (e.g. `self.root.opts`).
**Do NOT modify** these functions: List._bind_to_schema, BaseSchema.__init__, BaseSchema._init_fields, BaseSchema._bind_field
Edit ONLY src/marshmallow/fields.py and ONLY the buggy line above.
```

**Model output:**
```diff
-        or getattr(schema.opts, self.SCHEMA_OPTS_VAR_NAME)
+        or getattr(schema.root.opts, self.SCHEMA_OPTS_VAR_NAME)
```

Substitution `schema.root.opts` not `self.root.opts`. Both semantically equivalent — `schema` is a `List` instance at runtime, `Field.root` walks `.parent` chain to outer Schema either way.

**Result:**
- apply: succeeded at line 1114 with fuzz 1
- F2P: `test_datetime_list_inner_format` **PASSED**
- regression: **77 passed, 0 failed, 1 warning**

**What this proves:**
- The lever IS the suffix-text channel (not the latent pool)
- The lever IS controllable via graph-derived content (POSITION cells + traceback regex)
- The directive's needed channels are: (1) named target qname, (2) anti-target exclusion list, (3) buggy line content, (4) exception type + receiver + missing attr, (5) substitution mapping
- All channels except the substitution hint are 100% graph/issue-derived. The substitution hint "e.g. `self.root.opts`" is a graph-derived template using the parsed missing-attribute (`opts`) — also auto.

**Iter4 → iter5 delta (the line-level steering):**
iter4 had the target but edited the wrong line (the super call). iter5 added the buggy line content → model edited the right line. Line-level steering needs the source-line text in the directive.

**Cycle 0.2 deliverable shape:**
1. Ship `synth_traceback_target.rs --text-out` as a new pipeline stage between `synth_derived_notes` and the suffix assembly.
2. Modify `run_instance.py` to concat the directive into the suffix automatically.
3. Re-run on the 21-instance working set. Expectation: any instance with a Python traceback in the issue gets the same lever. Instances without tracebacks fall back to current behavior.

**Generalization risk:** This works for AttributeError + matching access-paths. Other exception classes (TypeError, KeyError, ValueError) need their own parsing + substitution-hint logic. Some instances don't have tracebacks at all and need a different signal. The directive shape is generic; the FEATURE EXTRACTORS need extension per exception class.

## iter4 — graph-derived deepest-target + anti-target · 2026-05-21 ~07:3X · 🔴 FAIL (wrong line within right function)

**Hypothesis:** iter3 listed 5 candidates and model picked first-listed. Picking ONE target (deepest traceback frame) + anti-target list should steer to right function.

**Result:**
- target: `DateTime._bind_to_schema` ✓ (graph control of target selection PROVEN)
- diff: `super()._bind_to_schema(field_name, schema)` → `super()._bind_to_schema(field_name, schema.root)`
- F2P: FAIL (edited the super call line at 1115, not the buggy line at 1117)
- regression: not fully checked, but apply succeeded

Conclusion: target selection works, but line-within-target needs additional signal → iter5.

## iter3 — graph-derived candidate list · 2026-05-21 ~07:1X · 🔴 FAIL (first-listed bias)

**Hypothesis:** graph-derived list of 5 candidate functions (from traceback POSITION matches) should let the model pick. Auto-derived via `--text-out` flag on synth_traceback_target.rs.

**Result:**
- target: `List._bind_to_schema` (the FIRST function listed; ordering effect)
- diff: refactored Nested-handling block at end of function (different wrong fix)
- F2P: FAIL

Conclusion: enumerate-candidates ≠ steer. Need to PICK ONE target. → iter4.

## iter2 — L2 graph-side traceback target cells · 2026-05-21 ~07:0X · 🔴 FAIL

**Hypothesis:** if the latent pool's score-weighting controls target selection, injecting 10 traceback-derived target cells at score=999 (vs prior max 0.28) should make the model fix `DateTime._bind_to_schema` (the actual bug-site) instead of `List._bind_to_schema` (the wrong default).

**Implementation:** new Rust bin `projection-text/src/bin/synth_traceback_target.rs`. Reads .gmap + issue.txt, regex-matches Python traceback lines (`File "...", line N, in fn`), looks up nodes by POSITION cell range containment, emits synth target cells. Graph-side, no hand-written instance text.

**Setup:**
- input pool: 224 entries, max existing score 0.28
- after augmentation: 234 entries, 10 new at score 999 (including `synth::Target::src::marshmallow::fields::DateTime::_bind_to_schema`)
- suffix: GENERIC (original "Produce a minimal unified git diff" template — NO prescriptive text)
- prefix: unchanged from iter0/iter1

**Result:**
- generation: 169 tok in 44.1s (3.8 tok/s)
- model output: **byte-identical to iter0 baseline + May 9 OG diff**. Same wrong-target rename of `List._bind_to_schema` line 633.
- apply: succeeded at line 630 with fuzz 3 (offset -2)
- F2P: ERROR (NameError — half-rename produces `super()._bind_to_schema(field_name, schema)` where `schema` is no longer in scope after the param rename)
- regression: 8 failed, 60 passed, 9 errors

**Finding (critical):**

The current pool-injection mechanism (`run_llama_pathB.py`: tokenize summary text → embed → mean-pool → splice into `batch.embd`) **does NOT transmit targeting signal strongly enough** to override the model's preference for easier-looking edits. Even at 999× score boost with the exact bug-site qname embedded as a target cell, the model produces the same wrong diff.

This bounds the entire latent-injection thesis as currently implemented:
- mean-pooled summary embeddings (1 vector per summary) lack the per-token attention granularity the model needs to be "directed" via latent
- score weighting in the JSON pool may be ignored entirely by the embed-and-splice path (need to verify if the runner even uses score for anything beyond ordering)
- the LATENT mechanism's max influence appears far below text-prefix or text-suffix influence

**Bounded conclusion:** glia's current "graph → pool → embed → splice" cannot steer target selection. Either we need a stronger latent mechanism (direct attention biasing, per-token injection), or we accept that glia's actionable lever is **graph-derived content in the PREFIX/SUFFIX text** (not via latent).

**Cycle 0.2+ revised directions:**

1. **graph-derived suffix** — extend an existing Rust bin (or write `synth_target_suffix.rs`) to emit a text block (not pool cell) that gets concatenated into the SUFFIX. The prescriptive suffix from iter1 worked; the win condition is auto-deriving that text from the graph (POSITION cells + access_paths). Output: text appended to suffix.txt. This is "graph-into-text-prefix" injection — different mechanism than pool.
2. **Verify pool-embed mechanism** — check if `run_llama_pathB.py` actually weights pool entries by score or just concatenates uniformly. If score is unused, that explains L2.
3. **Lens diagnosis on L2** — run the lens (slice 1.5 multi-position) on the L2 run vs baseline. If residuals are IDENTICAL between baseline-pool and L2-pool runs at every layer, that's empirical proof the pool injection has zero effect at the residual-stream level. If they differ but final-layer prediction is the same, the model is receiving the signal but not acting on it.

**Artefacts:**
- `inst-marshmallow-code__marshmallow-1359-7b-q4-L2-traceback/` — full L2 run dir
- `inst-marshmallow-code__marshmallow-1359-7b-q4-L2-traceback/traceback_matches.json` — debug record of matched (file, line, qname) tuples
- `projection-text/src/bin/synth_traceback_target.rs` — the graph-side bin (real glia code, not JSON edit)
- Memory: `[[project-marshmallow-l2-graph-side-result]]`, `[[feedback-no-json-for-injection]]`

## iter1 — prescriptive suffix · 2026-05-21 06:1X · 🟢 PASS

## iter1 — prescriptive suffix · 2026-05-21 06:1X · 🟢 PASS

**Hypothesis:** the model has the diagnosis (derived_notes already says `DateTime inside List → no .opts`) AND the access path (`self.root.opts` listed) in the prefix, but generic suffix lets it default to a wrong-target edit. Prescriptive suffix that names the function + the substitution should fix it.

**Change:** Replaced the default suffix.txt in `inst-marshmallow-code__marshmallow-1359-7b-q4-iter1-prescriptive/` with a prescriptive block:
- "Edit ONE function: `DateTime._bind_to_schema` in `src/marshmallow/fields.py`."
- "Use `self.root.opts` instead (Field.root walks .parent until outermost Schema)."
- "Edit ONLY src/marshmallow/fields.py. Do NOT modify List._bind_to_schema."

**Result:**
- generation: 108 tok in 57.9s (1.9 tok/s)
- model output:
```diff
-        or getattr(schema.opts, self.SCHEMA_OPTS_VAR_NAME)
+        or getattr(self.root.opts, self.SCHEMA_OPTS_VAR_NAME)
```
- targets `DateTime._bind_to_schema` correctly (line 1113 model / 1117 actual)
- apply: applied with `patch -p1 --forward --fuzz=10 -l` (whitespace + indent mismatch needed `-l`)
- F2P: `tests/test_fields.py::TestParentAndName::test_datetime_list_inner_format PASSED`
- regression suite (`pytest -rA tests/test_fields.py`): **77 passed, 0 failed, 1 warning**

**Comparison:**
| Run | apply | f2p | reg | reg_fail |
|---|---|---|---|---|
| Default suffix (4 prior history) | applied | FAIL | 76 | 1 |
| Default suffix (tonight 2026-05-21) | applied(fuzz) | FAIL | 60 | 16 |
| iter1 prescriptive | applied(fuzz) | PASS | 77 | 0 |

**Generalizable sage-feature derived:**
The prefix's `derived_notes` already contains "X has no Y attribute" + access-paths section lists candidate paths resolving to Y. Pipeline should auto-emit a **Required fix location** suffix block synthesizing these into a directive. The model has the diagnosis; it just needs to be told to *use* it.

Concrete: extend `run_instance.py::run_pipeline` (or wherever the prompt assembly is) to detect cases where derived_notes has an `X has no Y` pattern + access_paths contains a `self.Z.Y` resolution, then auto-emit a prescriptive suffix block. ~50 LOC in Python.

**Decision:** STOP iteration. Gold passes on marshmallow with iter1. The lever is real. Next cycle (0.2+) builds the auto-derivation. Wake-up state captured in INTERRUPT_STATE.md.

**Artefacts:**
- prefix: `inst-marshmallow-code__marshmallow-1359-7b-q4-iter1-prescriptive/prefix.txt` (UNCHANGED from default — proves prefix already had everything needed)
- suffix: `inst-marshmallow-code__marshmallow-1359-7b-q4-iter1-prescriptive/suffix.txt` (the prescriptive change)
- diff: `inst-marshmallow-code__marshmallow-1359-7b-q4-iter1-prescriptive/out.txt`
- healed patch: `/tmp/iter1.patch`

## iter0 — calibration baseline · 2026-05-21 06:0X · 🔴 FAIL

- Default suffix, current pipeline, fuzz=5 fix in run_instance.py:904 (after discovering GNU patch 2.8 default fuzz too strict)
- F2P FAIL (0/1), reg=60, reg_fail=16
- Model produced wrong-target diff (renamed param in List._bind_to_schema, not DateTime)
- See `[[reference-marshmallow-1359-diff-anatomy]]` for why
