# glia audit — 2026-06-10 (post-v0.4.16)

Five-agent file-grounded audit (incremental cache, determinism, domain-agnostic/pyo3
boundary, CODE_RULES compliance, graph quality/noise), seeded by repo-graph activation
at the orchestration level. Findings ranked impact × win. State at audit: main =
95d9788, v0.4.16 tagged, PyPI live.

NOTE on the live MCP: the repo-graph server (PID 12198, up since Jun 05) has a
*deleted* pre-gating engine .so mapped — likely the stale `repo-graph/rust` fork
wearing a 0.4.14 version string (zero `always_region` hits in that fork). It
regenerated `.ai/repo-graph/` on Jun 10 with 19,166 `.venv-eval` strings → the live
23,003-node graph is junk-weighted. Fresh 0.4.16 on this repo = 2,346 nodes, correctly
region-collapsed. Engine exonerated; ops + repo-graph-session fix.

## Tier 1 — HIGH impact, S fix (bundle as v0.4.17 patch cut)

| # | Finding | Where | Effort |
|---|---|---|---|
| 1 | Live MCP serving poisoned graph (stale fork .so + sticky cache) | ops: restart server, rm -rf .ai/repo-graph/; repo-graph session: git rm -r rust/, pin >=0.4.16 | S (ops) |
| 2 | Parse cache: RepoId not in cache key — build as `.` then `/abs/path` (or move repo) → reused nodes carry old RepoId, silently mixed-RepoId graph | engine/src/cache.rs:81, engine/src/lib.rs:75 | S (~10 LOC: store repo_canonical, discard on mismatch) |
| 3 | Parse cache: `go_module_prefix` not in key — go.mod module edit → stale Go parses forever, silently reintroduces WP-G leak | engine/src/lib.rs:344,767 | S |
| 4 | `CACHE_VERSION` stamps *engine crate* version (frozen 0.4.13); releases bump only py/Cargo.toml → upgrades never invalidate | engine/src/cache.rs:19 | S (stamp workspace/py version or build hash) |
| 5 | .gmap manifest has no engine-version stamp → poisoned/old caches survive engine upgrades (why #1 persists) | store/src/lib.rs:927-967 | S (mirror parse-cache stamp) |
| 6 | Shard order HashMap-random: `parses_by_lang` iteration decides shard indices → shard hashes flap per process, skip-unchanged never fires; also flaps nodes_json/dense-text section order | engine/src/lib.rs:391 → store/src/lib.rs:867 | S (sort keys) |
| 7 | `resolve_test_ids` first-match over qname_by_id HashMap — pick_primary bug class still live, exposed via py resolve_signal (WP-B) → activate seeds flap | graph/src/lib.rs:2239-2250 | S (collect + pick_primary) |
| 8 | `--no-incremental` / incremental=False doesn't purge sidecar — "force clean build" fixes one build then poisoned cache reused | cli/src/main.rs:507, py/src/lib.rs | S |
| 9 | Cross-edge emission order random in 8 resolvers (SharedSchema/Db/Cron/Config/Iac/Package `index.values()`; GraphQL/WS resolver_index) → cross_stack.gmap bytes not reproducible even same-process | graph/src/lib.rs:1379-1765 | S (sort cross_edges at end of run_all_resolvers, engine/src/lib.rs:455) |
| 10 | Byte-identical test too weak: fingerprint sorts nodes/edges, ignores cells/spans/docs — wouldn't catch #2/#3/#6. Replace with real bytes-on-disk compare after #6/#9 | engine/src/lib.rs:1590 | S |

Determinism stragglers to fold in: walk_dir unsorted (engine/src/lib.rs:1044),
build_scopes tiebreak (projection-text/src/lib.rs:164), activation top_k tiebreak on
id (activation/src/lib.rs:261), build_symbol_table iterates qname_by_id — CODE_RULES
§3 letter violation, latent (graph/src/lib.rs:222).

## Tier 2 — high value, M effort

| # | Finding | Where | Effort |
|---|---|---|---|
| 11 | pyo3 missing traversal layer: bfs/predecessors/reachable_by/qnames_exact/resolve_span unexposed; py neighbours() outgoing-only → MCP wrapper reimplements impact/trace in Python (cross-benefit anti-pattern) | graph/src/lib.rs:1877-2001 vs py/src/lib.rs:141-157 | M — its own WP |
| 12 | Intra-workspace import leak open for Rust/Java/C#/Python (only Go fixed in WP-G); glia's own repo_graph_* crates listed as external libs | code-domain/src/lib.rs:538-554 | M (read workspace members, Go pattern) |
| 13 | Duplicate CLASS+COMPONENT patched per-reader only; build_symbol_table last-write-wins lets edgeless framework marker shadow CLASS at build time | parsers/code/extractors/src/{angular,react,vue}.rs; graph/src/lib.rs:236 | M-L — decide at build-time merge with 0.5.0 registry work |
| 14 | generate_many/merge always passes cache:None — multi-repo builds get zero incremental benefit silently | engine/src/lib.rs:115 | M (when neuropil/Engram go multi-repo) |
| 15 | GenerateResult.parse_errors swallowed in py — wrapper can't report parse health | py/src/lib.rs:285-293 | S |
| 16 | Dual hand-rolled JSON projections (py manual escape vs cli serde shapes); escape_json misses control chars <0x20 → invalid JSON possible | py/src/lib.rs:74-139 vs cli/src/main.rs:252-289 | S-M (shared projection-json) |
| 17 | is_gmap_stale skip-list (5 names) divergent from walker gating — misses .venv-eval → walks ~12k files per status, spurious regens | store/src/lib.rs:936 | S (share walker gating) |

## Tier 3 — strategic (0.5.0)

- **store/ is the real 0.5.0 cost, not the rename.** Container embeds
  code_nav/symbols/unresolved_* (store/src/lib.rs:61-71); Header::for_code ships
  EMPTY registries — the registry-in-header model was never implemented; decode
  relies on compiled-in code-domain tables (store/src/lib.rs:310-323). Needs
  FORMAT_VERSION bump + opaque per-domain payload. Effort L.
- core/activation nearly clean: dead FlowKind enum (core/src/lib.rs:97-103, no
  consumers — delete), project_name knows Cargo.toml/package.json etc — relocate to
  code-domain/engine (core/src/lib.rs:263-385). S each.
- Engram --since (2c): sidecar already persists path→hash; just needs a pub iter/diff
  API on ParseCache. S when it lands.

## Tier 4 — hygiene (batch opportunistically)

- Clippy: ~87 warnings, 51× collapsible_if (extractors + fresh WP-D code). M, mostly --fix.
- Parse cache nits: fixed tmp name `.tmp` (concurrent-writer corruption, self-healing) — per-pid suffix; FileParse double-clone on put/get — Arc later; no negative caching of error files; sidecar bytes nondeterministic (HashMap bincode) — BTreeMap.
- Entry detection: no pyo3 #[pyfunction]/fn-main markers; route extractor self-noise (`GET .route(` from string literals); exclude list missing venv/.tox/.terraform/.mypy_cache etc; gitignore parser top-level-only + final-component matching (any dir named `repo-graph` collapses). S each; gitignore semantics M (or `ignore` crate).
- Docs: CLAUDE.md parser step 6 stale (engine owns parsers now, not py/Cargo.toml); WP-D "byte-identical" doc claims overstate until Tier-1 determinism lands.
- CODE_RULES compliance otherwise PASS: all 19 bins required-features=driver, kebab-case clean, zero panics reachable from pyo3 (one guarded unwrap store/src/lib.rs:564), engram-export decoupling holds, no TODO/FIXME in published path. Legacy `--out`-JSON synth bins are the §10 latent-JSON channel already slated for retirement.

## Recommended sequence

1. Ops fix #1 today (restores honest graph queries for everything downstream).
2. v0.4.17 patch PR: Tier 1 #2-10 + stragglers (~100-150 LOC, restores both shipped
   guarantees — byte-identical and determinism — and makes the gate test honest).
3. #11 traversal pyo3 as its own WP (unblocks wrapper de-duplication).
4. #12 import leak next emission-quality WP.
5. #13 + store redesign folded into 0.5.0 planning.
